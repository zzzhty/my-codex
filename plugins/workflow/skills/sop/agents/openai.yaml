interface:
  display_name: "SOP"
  short_description: "Create, update, or execute reusable procedures"
  default_prompt: "Use $sop to create, update, validate, or execute a reusable procedure with explicit trigger, inputs, execution harness, permissions, ordered steps, outputs, validation evidence, allowed and forbidden actions, stop conditions, escalation, failure handling, and durable writeback."
