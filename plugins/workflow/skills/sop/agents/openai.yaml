interface:
  display_name: "SOP"
  short_description: "Create, update, or execute reusable procedures"
  default_prompt: "Use $sop to create, update, validate, or execute a reusable standard operating procedure with explicit trigger, inputs, execution harness, ordered steps, outputs, validation evidence, allowed and forbidden actions, stop conditions, escalation, and durable writeback."
