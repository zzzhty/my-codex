interface:
  display_name: "Long Running Goal"
  short_description: "Create, execute, or close continuation-ready staged goals"
  default_prompt: "Use $long-running-goal to create, execute, resume, continue, evolve, or close a staged goal with a reusable continuation contract, milestones, Loop Blueprint harness boundaries, validation gates, checkpoint evidence, autonomous continuation after passed gates, explicit permission stop conditions, request-supersession checks, strategy/plan evolution, active goal-state hygiene, and clean active/archive sync."
