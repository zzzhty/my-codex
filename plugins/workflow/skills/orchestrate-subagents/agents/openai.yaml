interface:
  display_name: "Orchestrate Subagents"
  short_description: "Coordinate user-requested Codex subagents"
  default_prompt: "Use $orchestrate-subagents to split this complex task into bounded subagent assignments, wait for results, and consolidate evidence, blockers, risks, validation gaps, and next actions."
