interface:
  display_name: "Skill Compressor"
  short_description: "Compress skills without semantic drift"
  default_prompt: "Use $skill-compressor to reduce a skill or plugin instruction footprint while preserving operational semantics with snapshots, validation, and independent review."
